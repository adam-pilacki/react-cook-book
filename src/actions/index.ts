import { addRecipe, updateRecipe, deleteRecipe } from './recipeActions';

/**
 * For now only recipeCrud actions are here
 * but add here any other actions that will be added in the future
 * (if there will be any future ;>)
 */

export { addRecipe, updateRecipe, deleteRecipe };